let variable = 0,
    userGuess = {};


module.exports = {variable, userGuess}

// Created quiz object.

// Retrieve the questions

// Populate the quiz

// On submit, should get qnsIdx and guessIdx

// Send the idx to db and get back the ans
